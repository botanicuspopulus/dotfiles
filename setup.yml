---
- name: Localhost setup
  hosts: localhost
  vars:
    desktop_wm: "Hyprland"
    dotfiles_dir: "{{ ansible_env.HOME }}/dotfiles"
    dev_path: "{{ ansible_env.HOME }}/Development"

  roles:
    - setup
    - packages
    - pipewire
    - gpu
    - stow
    - python
    - user
    - greetd
