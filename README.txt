# How to get this up and running (I Think)
1. Create a .zshenv file with the following content:

  export ZDOTDIR=~/.config/zsh
  [[ -f $ZDOTDIR/.zshenv ]] && . $ZDOTDIR/.zshenv

2. Create a zshrc with the following content:
  
