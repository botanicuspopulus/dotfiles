from ranger.gui.color import BRIGHT as BRIGHT, bold as bold, default_colors as default_colors, reverse as reverse
from ranger.gui.colorscheme import ColorScheme as ColorScheme

class Snow(ColorScheme):
    def use(self, context): ...
